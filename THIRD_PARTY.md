# Third-party notices

Featherframe's own code, the plate crosswalk (`server/scripts/folios/`),
and the legend transcriptions (`server/scripts/legends.yaml`) are licensed
under the Apache License 2.0 (`LICENSE`). Everything below belongs to someone
else and keeps its own terms.

## Bundled in this repository

| What | Where | Licence |
|---|---|---|
| Pinyon Script (Nicole Fally) | `server/featherframe/fonts/PinyonScript-Regular.ttf` | SIL OFL 1.1, `OFL-PinyonScript.txt` |
| IM Fell Double Pica SC (Igino Marini, Reserved Font Name "IM FELL Double Pica SC") | `server/featherframe/fonts/IMFellDoublePicaSC.ttf` | SIL OFL 1.1, `OFL-IMFell.txt` |
| EB Garamond (The EB Garamond Project Authors) | `server/featherframe/fonts/EBGaramond*.ttf` | SIL OFL 1.1, `OFL.txt` |
| Inter (The Inter Project Authors) | `server/featherframe/fonts/Inter-*.otf`, `firmware/tools/screens/fonts/Inter-Medium.otf` | SIL OFL 1.1, `OFL-Inter.txt`, `firmware/tools/screens/fonts/OFL.txt` |
| A subset of Pinyon Script as WOFF | `firmware/src/ff_portal_font.h` (generated by `firmware/tools/portal_font.py`) | SIL OFL 1.1 |

The fonts are used unmodified apart from that subset. The baked boot screens
(`firmware/src/ff_screens*.h`), the favicon, and the example images are
renderings made with these fonts, not font software.

## Artwork

- **Audubon's plates.** The Havell edition of *The Birds of America* is in the
  public domain. The scans come from the National Audubon Society by way of
  [nathanbuchar/audubon-bird-plates](https://github.com/nathanbuchar/audubon-bird-plates),
  and are redistributed in this repo's `plates-v1` release. Credit line to keep
  when displaying them: *Courtesy of the John James Audubon Center at Mill
  Grove, Montgomery County Audubon Collection, and Zebra Publishing.* The
  plates themselves are not in the git tree.
- **The hawthorn bough and the boot-screen wrens** (`server/featherframe/art/`,
  `firmware/tools/screens/art/`) were generated for this project with OpenAI's
  image models, using the public-domain plates as style references.

## Fetched at build or install time (not in this repository)

Firmware (PlatformIO `lib_deps`, `firmware/platformio.ini`):

| Library | Licence |
|---|---|
| [Seeed_GFX](https://github.com/Seeed-Studio/Seeed_GFX) (a fork of Bodmer's TFT_eSPI) | MIT and FreeBSD portions; see its `license.txt` |
| [WiFiManager](https://github.com/tzapu/WiFiManager) | MIT |
| [Arduino-ESP32](https://github.com/espressif/arduino-esp32) via the pioarduino platform | LGPL-2.1 (core), Apache-2.0 (ESP-IDF) |

`firmware/lib/driver/driver.h` is this project's own panel selection header
for Seeed_GFX, not vendored code.

Server (`server/requirements.txt`, installed by pip into a venv):

| Package | Licence |
|---|---|
| FastAPI, PyYAML | MIT |
| Uvicorn, Jinja2, NumPy | BSD-3-Clause |
| Pillow | MIT-CMU (HPND) |
| Requests, python-multipart | Apache-2.0 |
| zeroconf | LGPL-2.1-or-later (used as an unmodified, separately installed library) |

BirdNET-Pi, BirdNET-Go and BirdWeather are separate programs that Featherframe
reads detections from; none of their code is included here.
