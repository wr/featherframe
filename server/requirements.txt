# Featherframe server dependencies.
# Kept deliberately lean — this runs alongside BirdNET-Pi's tflite analyzer,
# possibly on a Pi Zero 2W (512MB). Targets Python 3.9+.
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
pillow>=10.2
numpy>=1.26
pyyaml>=6.0
requests>=2.31
python-multipart>=0.0.9
zeroconf>=0.130      # mDNS: advertise _featherframe._tcp so the frame finds the server
websockets>=10.4     # uvicorn serves the frames' push socket (W-841) through it
